package P8;

import JAVARuntime.AnimationFile;
import JAVARuntime.AnimationMaskFile;
import JAVARuntime.Color;
import JAVARuntime.ColorGradient;
import JAVARuntime.Component;
import JAVARuntime.Curve;
import JAVARuntime.FloatSlider;
import JAVARuntime.FontFile;
import JAVARuntime.IntSlider;
import JAVARuntime.Map2;
import JAVARuntime.Matcap;
import JAVARuntime.MaterialFile;
import JAVARuntime.ObjectFile;
import JAVARuntime.OutPFile;
import JAVARuntime.OutputVideoFile;
import JAVARuntime.Point2;
import JAVARuntime.Point3;
import JAVARuntime.ProjectFile;
import JAVARuntime.PropertiesButton;
import JAVARuntime.SoundFile;
import JAVARuntime.SpatialObject;
import JAVARuntime.Texture;
import JAVARuntime.TextureFile;
import JAVARuntime.Vector2;
import JAVARuntime.Vector3;
import JAVARuntime.VertexFile;
import JAVARuntime.VideoFile;
import JAVARuntime.WorldFile;

public class g {

    public static h[] f21161a = {Color.inspectorController(), OutPFile.inspectorController(), Vector3.inspectorController(), Vector2.inspectorController(), FloatSlider.inspectorController(), IntSlider.inspectorController(), Texture.inspectorController(), Matcap.inspectorController(), Map2.inspectorController(), SpatialObject.inspectorController(), Component.inspectorController(), Point3.inspectorController(), Point2.inspectorController(), PropertiesButton.inspectorController(), ObjectFile.inspectorController(), MaterialFile.inspectorController(), WorldFile.inspectorController(), SoundFile.inspectorController(), VideoFile.inspectorController(), OutputVideoFile.inspectorController(), TextureFile.inspectorController(), VertexFile.inspectorController(), FontFile.inspectorController(), Curve.inspectorController(), ColorGradient.inspectorController(), ProjectFile.inspectorController(), AnimationFile.inspectorController(), AnimationMaskFile.inspectorController()};

    public static l[] f21162b = {Color.genericInterface(), OutPFile.genericInterface(), Vector3.genericInterface(), Vector2.genericInterface(), Texture.genericInterface(), Matcap.genericInterface(), ObjectFile.genericInterface(), MaterialFile.genericInterface(), WorldFile.genericInterface(), SoundFile.genericInterface(), VideoFile.genericInterface(), OutputVideoFile.genericInterface(), TextureFile.genericInterface(), VertexFile.genericInterface(), FontFile.genericInterface(), Curve.genericInterface(), ColorGradient.genericInterface(), ProjectFile.genericInterface(), AnimationFile.genericInterface(), AnimationMaskFile.genericInterface()};

    public static h[] a() {
        return f21161a;
    }

    public static l[] b() {
        return f21162b;
    }
}
