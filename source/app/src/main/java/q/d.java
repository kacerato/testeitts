package Q;

import C.h;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import com.bumptech.glide.load.engine.t;

public class d implements e<P.c, byte[]> {
    @Override
    @Nullable
    public t<byte[]> a(@NonNull t<P.c> tVar, @NonNull h hVar) {
        return new M.b(Y.a.d(tVar.get().c()));
    }
}
