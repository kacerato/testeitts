package u;

import com.android.tools.r8.graph.M2;
import java.util.Comparator;

public final class K0 implements Comparator {
    @Override
    public final int compare(Object obj, Object obj2) {
        return ((M2) obj).compareTo((M2) obj2);
    }
}
