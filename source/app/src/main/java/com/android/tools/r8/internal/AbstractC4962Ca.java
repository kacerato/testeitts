package com.android.tools.r8.internal;

import java.util.function.IntConsumer;

public abstract class AbstractC4962Ca {
    public static void a(IntConsumer intConsumer) {
        intConsumer.accept(178);
        intConsumer.accept(179);
        intConsumer.accept(180);
        intConsumer.accept(181);
    }

    public static void b(IntConsumer intConsumer) {
        intConsumer.accept(182);
        intConsumer.accept(183);
        intConsumer.accept(184);
        intConsumer.accept(185);
    }

    public static void a(IntConsumer intConsumer, IntConsumer intConsumer2) {
        intConsumer.accept(192);
        intConsumer.accept(193);
        intConsumer.accept(197);
        intConsumer.accept(187);
        intConsumer.accept(188);
        intConsumer.accept(189);
        intConsumer2.accept(206);
    }
}
