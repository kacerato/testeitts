package Sf;

import Wf.n;
import org.jetbrains.annotations.NotNull;

public interface f<T, V> extends e<T, V> {
    @Override
    V a(T t10, @NotNull n<?> nVar);

    void b(T t10, @NotNull n<?> nVar, V v10);
}
