package pn;

import java.io.IOException;
import java.io.OutputStream;
import java.nio.ByteOrder;
import jn.y;

public class i extends a {
    public void h(OutputStream stream, y params, ByteOrder order) throws IOException {
        b(stream, params.f94387a[0], order);
        b(stream, params.f94387a[1], order);
        b(stream, params.f94387a[2], order);
        b(stream, params.f94388b, order);
        b(stream, params.f94389c, order);
        c(stream, params.f94390d, order);
        c(stream, params.f94391e, order);
    }
}
